# Authentication Service Configuration

# Server Configuration
PORT=80

# Database Configuration (PostgreSQL)
DSN=host=postgres port=5432 user=postgres password=password dbname=users sslmode=disable timezone=UTC connect_timeout=5

# Alternative DSN format
# DB_HOST=postgres
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=password
# DB_NAME=users
# DB_SSLMODE=disable

# Logger Service Configuration (for audit logging)
LOGGER_SERVICE_URL=http://logger-service:80

# JWT Configuration (future enhancement)
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRY_HOURS=24

# Password Security
BCRYPT_COST=10

# Application Settings
LOG_LEVEL=info
ENVIRONMENT=development

# CORS Settings
ALLOWED_ORIGINS=http://localhost:8081,http://frontend:8081

# Notes:
# - Change DB_PASSWORD in production
# - Never commit .env file to Git
# - Use strong JWT_SECRET in production
