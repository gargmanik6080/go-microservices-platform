# Mail Service Configuration

# Server Configuration
PORT=80

# SMTP Configuration
MAIL_DOMAIN=localhost
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=none
# Options: none, tls, ssl

# Sender Configuration
FROM_NAME=Your Name
FROM_ADDRESS=noreply@example.com

# Production SMTP Examples:

# Gmail SMTP
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_ENCRYPTION=tls

# SendGrid
# MAIL_HOST=smtp.sendgrid.net
# MAIL_PORT=587
# MAIL_USERNAME=apikey
# MAIL_PASSWORD=your-sendgrid-api-key
# MAIL_ENCRYPTION=tls

# Mailgun
# MAIL_HOST=smtp.mailgun.org
# MAIL_PORT=587
# MAIL_USERNAME=postmaster@your-domain.com
# MAIL_PASSWORD=your-mailgun-password
# MAIL_ENCRYPTION=tls

# AWS SES
# MAIL_HOST=email-smtp.us-east-1.amazonaws.com
# MAIL_PORT=587
# MAIL_USERNAME=your-ses-smtp-username
# MAIL_PASSWORD=your-ses-smtp-password
# MAIL_ENCRYPTION=tls

# Email Templates
TEMPLATE_PATH=/templates
USE_HTML_TEMPLATES=true

# Email Queue Settings
QUEUE_ENABLED=false
MAX_QUEUE_SIZE=1000

# Retry Configuration
MAX_SEND_RETRIES=3
RETRY_DELAY_SECONDS=5

# Rate Limiting
MAX_EMAILS_PER_MINUTE=100

# Logging
LOG_LEVEL=info
ENVIRONMENT=development

# Development Settings (MailHog)
MAILHOG_WEB_UI=http://localhost:8025

# Notes:
# - MailHog is used for development (catches all emails)
# - View emails at http://localhost:8025 in development
# - Update SMTP settings for production email provider
# - Use app-specific passwords for Gmail
# - Consider using dedicated email services (SendGrid, Mailgun, SES) for production
